{
  "name": "svgi-site",
  "version": "1.0.0",
  "description": "SVGI web site",
  "private": true,
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "server": "http-server ./build",
    "publish": "gulp publish",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Angel M Miguel <angel@laux.es>",
  "license": "Apache-2.0",
  "dependencies": {
    "gulp": "^3.9.1",
    "gulp-ejs": "^3.0.1",
    "gulp-gh-pages": "^0.5.4",
    "gulp-sass": "^3.1.0",
    "http-server": "^0.10.0",
    "marked": "^0.3.6"
  }
}
